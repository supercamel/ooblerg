--- a/src/meson.build
+++ b/src/meson.build
@@ -425,12 +425,17 @@ if introspection
 
    if get_option('vapi')
 
+     libadwaita_vapi_gir_dirs = [
+       dependency('gobject-introspection-1.0').get_variable(pkgconfig: 'girdir'),
+     ]
+
      libadwaita_vapi = gnome.generate_vapi(package_api_name,
              sources: libadwaita_gir[0],
             packages: [ 'gio-2.0', 'gtk4' ],
              install: true,
          install_dir: vapidir,
        metadata_dirs: [ meson.current_source_dir() ],
+            gir_dirs: libadwaita_vapi_gir_dirs,
      )
 
    endif
